
var multiple = new Multiple({
    selector: '.item',
    background: 'linear-gradient(grey, black)',
});

var multiple = new Multiple({
    selector: '.nameBox',
    background: 'linear-gradient(grey, black)',
});