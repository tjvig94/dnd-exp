
var multiple = new Multiple({
    selector: '.item',
    background: 'linear-gradient(black, grey)',
});

var multiple = new Multiple({
    selector: '.nameBox',
    background: 'linear-gradient(black, grey)',
});